# 左手单手输入方案设计文档

## 一、背景与动机

### 1.1 现有方案的不足

| 方案 | 核心问题 |
|---|---|
| 主流 PC 输入法（搜狗/微软拼音/百度） | 不支持九宫格 T9，数字键就是数字，无单手优化 |
| 小白T9（基于 Rime） | 依赖物理小键盘 NumPad，无小键盘时体验差，键位不可自定义 |
| 九宫格 T9（通用） | 学习成本高，需记忆数字→拼音映射，候选选择仍需右手辅助 |
| Half-QWERTY 镜像 | 仅解决英文字母层，中文输入法适配差，翻页选词不便 |
| Frogpad / OPRIQ | 需重新学习全新键位布局，中文输入未专门优化 |

### 1.2 核心洞察

> **键盘本身就是左右对称设计的**——QWERT 对应 POIUY、ASDF 对应 LKJH、ZXCV 对应 M,./。
> 利用已有的肌肉记忆，通过"双击"触发对称键，实现零学习成本的单手输入。

---

## 二、方案概述

### 2.1 设计理念

**"时间维度的键位复用"**——同一个物理按键，单击输出原键，快速双击输出对称键。

- ✅ **零学习成本**：不需要记忆任何新键位，肌肉记忆直接复用
- ✅ **输入法无感**：输出的是标准字母键码，任何输入法都能正常解析
- ✅ **左右手通用**：对称映射天然支持左手或右手单手操作
- ✅ **一键切换**：通过快捷键在"正常模式"和"单手模式"间瞬时切换
- ✅ **可视化辅助**：透明预览窗口帮助熟悉阶段快速上手

### 2.2 交互规则

| 操作 | 行为 | 示例 |
|---|---|---|
| **单击**按键 | 输出原键 | 按 `a` → 输出 `a` |
| **快速双击**同一按键（220ms 内） | 输出该键的对称键 | 双击 `a` → 输出 `p` |
| **按 CapsLock** | 切换单手模式开/关 | 全局一键切换 |
| **单手模式关闭** | 键盘完全恢复正常 | 所有按键原样输出 |

### 2.3 对称键映射表

#### 字母键（主键盘区）

| 行 | 左侧键 → 对称键 | 物理位置对应 |
|---|---|---|
| **上排** | `Q↔P` `W↔O` `E↔I` `R↔U` `T↔Y` | 左手小指↔右手小指，依此类推 |
| **中排** | `A↔'` `S↔L` `D↔K` `F↔J` `G↔H` | 左手基准行↔右手基准行 |
| **下排** | `Z↔/` `X↔.` `C↔,` `V↔M` `B↔N` | 左手下排↔右手下排 |

#### 映射关系图

```
左半区                    右半区
┌─┬─┬─┬─┬─┐         ┌─┬─┬─┬─┬─┐
│Q│W│E│R│T│         │Y│U│I│O│P│
└─┴─┴─┴─┴─┘         └─┴─┴─┴─┴─┘
  ↕ ↕ ↕ ↕ ↕           ↕ ↕ ↕ ↕ ↕
┌─┬─┬─┬─┬─┐         ┌─┬─┬─┬─┬─┐
│A│S│D│F│G│         │H│J│K│L│'│
└─┴─┴─┴─┴─┘         └─┴─┴─┴─┴─┘
  ↕ ↕ ↕ ↕ ↕           ↕ ↕ ↕ ↕ ↕
┌─┬─┬─┬─┬─┐         ┌─┬─┬─┬─┬─┐
│Z│X│C│V│B│         │N│M│,│.│/│
└─┴─┴─┴─┴─┘         └─┴─┴─┴─┴─┘
```

> **使用方式**：左手放在键盘左侧，单击打左边字母，双击打右边字母。右手同理。

---

## 三、技术实现

### 3.1 技术选型

| 选项 | 选择 | 理由 |
|---|---|---|
| 实现语言 | **AutoHotkey v2** | 原生支持全局键盘钩子、时间判定、GUI 窗口；脚本即改即生效；单文件分发 |
| 替代方案 | C++ / C# | 性能更好但开发成本高，本方案无性能瓶颈，AHK 足够 |
| 分发方式 | `.ahk` 源码 + `Ahk2Exe` 编译为 exe | 零依赖，双击即用 |

### 3.2 系统架构

```
┌─────────────────────────────────────────────────────┐
│                  用户按键事件                        │
└─────────────────────┬───────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────┐
│          全局低级键盘钩子（AHK #HotIf）              │
│    拦截所有按键 → 判断单手模式是否激活               │
└─────────────────────┬───────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────┐
│              双击判定引擎                            │
│  • 记录 LastKey 和 LastPressTime                    │
│  • A_TimeSincePriorHotkey < 220ms → 双击            │
│  • 超时未双击 → 输出原键（单击）                    │
└────────┬─────────────────────────┬─────────────────┘
         ▼                         ▼
┌────────────────┐       ┌──────────────────┐
│  单击 → 原键   │       │ 双击 → 对称键    │
│  SendInput(c)  │       │  SendInput(p)    │
└────────────────┘       └──────────────────┘
         │                         │
         ▼                         ▼
┌─────────────────────────────────────────────────────┐
│              预览窗口更新                            │
│  • 显示原始键（小字）+ 映射键（大字）               │
│  • 跟随光标位置，自动避开屏幕边缘                    │
│  • 半透明、无边框、置顶、不抢焦点                   │
└─────────────────────────────────────────────────────┘
```

### 3.3 核心算法

#### 双击判定逻辑

```
onKeyDown(key):
    now = A_TickCount
    if key == LastKey and (now - LastPressTime) < DOUBLE_CLICK_TIME:
        // 双击命中 → 输出对称键
        SendInput(SymmetricMap[key])
        ResetState()
    else:
        // 第一次按下 → 记录并等待
        LastKey = key
        LastPressTime = now
        SetTimer(SingleKeyTimeout, DOUBLE_CLICK_TIME)

SingleKeyTimeout():
    if LastKey is still pending:
        // 超时 → 视为单击，输出原键
        SendInput(LastKey)
        ResetState()
```

#### 预览窗口定位算法

```
getCaretPosition():
    pos = GetCaretPos()  // 优先获取文本插入符位置
    if pos == (0, 0):
        pos = GetMousePos()  // 回退到鼠标位置
    return pos

updateWindowPosition():
    x = caretX + OFFSET_X
    y = caretY + OFFSET_Y
    // 边界检测
    if x + winW > screenRight:  x = screenRight - winW - 10
    if y + winH > screenBottom: y = screenBottom - winH - 10
    MoveWindow(x, y)
```

### 3.4 一键切换实现

- **热键**：`CapsLock`（可通过配置改为 `F12`、`RCtrl` 等）
- **切换行为**：
  - 按一下 → 进入单手模式（预览窗口显示，钩子激活）
  - 再按一下 → 退出单手模式（预览窗口隐藏，钩子停用）
- **状态指示**：托盘图标颜色变化 + `TrayTip` 气泡提示

---

## 四、预览窗口设计

### 4.1 视觉规范

| 属性 | 值 |
|---|---|
| 背景色 | 白色（透明度 220/255） |
| 边框 | 细灰边 |
| 置顶 | 是（`+AlwaysOnTop`） |
| 抢焦点 | 否（`NA` 参数） |
| 映射键字体 | Segoe UI Bold 12pt（大字） |
| 原始键字体 | Segoe UI 8pt（小字，前缀 "←"） |
| 默认位置 | 光标右下方偏移 (20, 30) px |

### 4.2 显示内容

| 场景 | 映射键区域 | 原始键区域 |
|---|---|---|
| 单击 `a` | `a`（大字） | 空 |
| 双击 `a` | `p`（大字） | `← a`（小字） |
| 模式关闭 | 隐藏 | 隐藏 |

### 4.3 位置策略

1. 优先获取**文本插入符（Caret）**位置
2. 若失败（无焦点窗口），回退到**鼠标光标**位置
3. 始终偏移 (20, 30) 避免遮挡当前输入位置
4. 每 100ms 刷新一次位置（跟随光标移动）
5. 边界检测：确保窗口不超出屏幕工作区域

---

## 五、配置项

### 5.1 可调参数

| 参数 | 默认值 | 说明 |
|---|---|---|
| `DOUBLE_CLICK_TIME` | 220 ms | 双击判定时间窗口，越小越灵敏但易误触 |
| `SYMMETRY_HOTKEY` | CapsLock | 切换单手模式的热键 |
| `PREVIEW_WIN_WIDTH` | 120 px | 预览窗口宽度 |
| `PREVIEW_WIN_HEIGHT` | 70 px | 预览窗口高度 |
| `OFFSET_X` | 20 px | 窗口距光标的水平偏移 |
| `OFFSET_Y` | 30 px | 窗口距光标的垂直偏移 |
| `TRANSPARENCY` | 220 | 窗口透明度（0=全透，255=不透） |

### 5.2 进阶配置（未来扩展）

- 双击灵敏度滑块（UI 设置面板）
- 映射表可视化编辑器
- 按应用程序白名单/黑名单
- 左手模式 / 右手模式切换
- 导出/导入用户配置（Ini 格式）

---

## 六、使用场景

### 6.1 目标用户

| 用户类型 | 使用方式 |
|---|---|
| 右手受伤/不便者 | 左手单击打左半键，双击打右半键 |
| 怀抱婴儿的父母 | 单手抱娃，另一手打字 |
| 习惯手机九宫格的中老年用户 | 迁移到 PC 后的过渡方案 |
| 小配列键盘用户（60%/65%） | 无小键盘，左手单手应急输入 |
| 程序员左手写代码 | 左手编码 + 右手鼠标，双击补右手符号 |

### 6.2 典型使用流程

```
1. 启动脚本（开机自启可选）
2. 正常打字（单手模式关闭，零干扰）
3. 需要单手输入时 → 按 CapsLock 激活
4. 预览窗口出现，左手放在键盘左侧
5. 单击打左边字母，双击打右边字母
6. 输入完成后 → 再按 CapsLock 退出
```

---

## 七、已知限制与应对

| 限制 | 影响 | 应对策略 |
|---|---|---|
| 双击延迟（最高 220ms） | 快速打字时有轻微滞后感 | 可调低阈值至 150ms；熟练后延迟感消失 |
| 高频双字母误触（如 `ll`） | 连续输入同字母可能误触发对称键 | 可加入白名单跳过特定组合；或要求双击必须落在不同键 |
| 游戏兼容性 | 部分反作弊系统拦截 AHK | 管理员身份运行；或加入进程白名单 |
| 仅支持 Windows | AHK 是 Windows 专属 | 跨平台需另写实现（macOS 用 Karabiner-Elements） |
| 功能键未映射 | Shift/Ctrl/Alt/Space/Enter 不做对称 | 这些键保持原样，不影响正常使用 |

---

## 八、未来扩展方向

### 8.1 功能增强

- [ ] **设置面板 UI**：C# WinForms 或 AHK GUI 做可视化配置
- [ ] **映射表编辑器**：图形化拖拽修改对称关系
- [ ] **多套映射方案**：QWERTY / Dvorak / Colemak 各自有对称映射
- [ ] **统计与学习**：记录常用组合，自动调整双击灵敏度
- [ ] **声音反馈**：单击/双击时播放不同音效，强化肌肉记忆

### 8.2 平台扩展

- [ ] **macOS 版**：基于 Karabiner-Elements 的 complex_modifications
- [ ] **Linux 版**：基于 xkb 或 interception-tools
- [ ] **移动端**：iOS/Android 外接键盘时的单手模式

### 8.3 社区化

- [ ] 开源仓库（GitHub/Gitee）
- [ ] 插件市场（用户分享自定义映射方案）
- [ ] 无障碍认证（服务残障用户群体）

---

## 九、附录

### 9.1 完整键位映射表

```yaml
# 左手键 → 右手对称键
Q: P
W: O
E: I
R: U
T: Y
Y: T
U: R
I: E
O: W
P: Q

A: "'"
S: L
D: K
F: J
G: H
H: G
J: F
K: D
L: S
': A

Z: /
X: .
C: ,
V: M
B: N
N: B
M: V
,: C
.: X
/: Z
```

### 9.2 技术依赖

| 依赖 | 版本 | 用途 |
|---|---|---|
| AutoHotkey | v2.0+ | 脚本运行环境 |
| Windows | 10/11 | 目标操作系统 |
| Segoe UI 字体 | 系统自带 | 预览窗口渲染 |

### 9.3 参考资源

- AutoHotkey v2 官方文档：https://www.autohotkey.com/docs/v2/
- Rime 输入法引擎：https://rime.im/
- 小白T9 开源仓库：https://github.com/HuanSoft-Open-Source-Community/xiaobai-t9
- Half-QWERTY 研究论文：Matias, 1995

---

> **文档版本**：v1.0  
> **最后更新**：2026-07-25  
> **许可证**：GPLv3（与 AutoHotkey 一致）
