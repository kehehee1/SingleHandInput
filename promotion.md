# 左手单手输入工具 — 让右手解放，效率翻倍

> 一个 AutoHotkey v2 小工具，单击输出原键，双击输出对称键，让左手完成全部键盘操作

## 为什么需要这个工具？

日常工作中，右手频繁在键盘和鼠标之间切换，每次伸手去拿鼠标 → 定位 → 点击 → 回到键盘，至少浪费 1-2 秒。

如果一天切换 200 次，就是 **3-5 分钟** 白白浪费。更不用说频繁移动带来的手腕疲劳。

**如果把键盘左半区的按键，通过双击映射到右半区呢？**

这就是这个工具的核心思路：**左手负责所有键盘输入，右手专心操作鼠标/轨迹球/触控板。**

## 怎么工作？

- **单击**任何键 → 输出原键（正常打字，完全无感）
- **快速双击**同一键（220ms 内）→ 输出映射的对称键

比如双击 `a` → 退格（Backspace），双击 `z` → 回车（Enter），双击 `1` → `0`……

### 默认映射

```
数字行:  1→0  2→9  3→8  4→7  5→6
上排:    q→p  w→o  e→i  r→u  t→y
中排:    a→Backspace  s→l  d→k  f→j  g→h
下排:    z→Enter  x→Delete  c→m  v→n
```

> 完整映射可自定义，双击 `a` 退格、双击 `z` 回车是最实用的两个——不用再伸手去够小拇指键位。

## 亮点功能

### 1. 虚拟键盘预览窗

开启后屏幕右下角显示半透明虚拟键盘，蓝色键帽表示有映射，灰色表示无映射。

- 敲击键帽会高亮闪烁，反馈清晰
- 鼠标悬浮显示映射目标键名
- 可拖动到任意位置，自动保存
- 支持全键盘 / 仅映射键两种预览模式

### 2. 状态持久化

所有配置（单手模式开关、预览窗显隐、窗口位置、预览模式）**重启后自动恢复**，不用每次手动设置。

### 3. 与 Pet 工具自动兼容

如果你在用 [Pet](https://github.com/majorworld/Pet)（Space/CapsLock 增强菜单），本工具会自动检测并让出控制权，两者互不干扰。

### 4. 组合键完全穿透

按住 Ctrl/Alt/Shift/Win 时按键正常穿透，不影响 Ctrl+C、Ctrl+V 等组合键。

## 我的使用场景

- **写代码**：左手键盘 + 右手鼠标选代码、点菜单，不用频繁伸手
- **浏览网页**：右手鼠标滚轮 + 左手快捷键，效率提升明显
- **CAD/设计软件**：右手绘图/选工具，左手输入命令

配合 **StrokeIt**（鼠标手势工具），右手一个手势 = 关闭标签页、刷新、前进后退，基本不用离开鼠标。

## 快速上手

1. 安装 [AutoHotkey v2](https://www.autohotkey.com/download/)
2. 下载脚本，双击 `LeftHandSymmetry.ahk` 运行
3. 按 **Alt+R** 切换单手模式
4. 试试双击 `a`（退格）、双击 `z`（回车）—— 马上就能感受到区别

> 项目地址：https://github.com/yourusername/SingleHandInput （待补充）

## 写在最后

这个小工具花了两天时间做出来，从最初的简单映射，到现在的预览窗口、配置持久化、冲突规避，逐步打磨完善。

如果你也有右手频繁切换键盘鼠标的痛点，不妨试试。

欢迎 Star、提 Issue、贡献代码！
